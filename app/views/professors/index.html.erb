<h2>Listing professors</h2>
 <%= link_to 'Adding new professor', new_professor_path %>
<table>
  <% @professors.each do |professor| %>
    <tr>
      <td><%= professor.prof_name %></td>
    </tr>
  <% end %>
</table>
